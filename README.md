# AI Obstacle Avoidance Robot + Real-Time Digital Twin

Minimal autonomy (FSM + hysteresis) for obstacle avoidance, with a Python digital twin that:
- visualizes robot motion live
- logs telemetry to CSV
- generates validation plots automatically

## Run (no hardware required)
From the project root:

```bash
python3 sim/sim.py